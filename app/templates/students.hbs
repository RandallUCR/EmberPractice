<div class="row">
    <h1 class="col-md-12">Students List</h1>
    <div class="col-md-12">
        <LinkTo @route='add-student'>Add</LinkTo>
        {{#each @model as |student|}}
        <StudentsList @name = {{student.name}} @id = {{student.id}}/>

        {{/each}}     
    </div>
</div>



{{outlet}}