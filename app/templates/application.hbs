{{!-- The following component displays Ember's default welcome message. --}}
<LinkTo @route='students'>Students</LinkTo>
<LinkTo @route='courses'>Courses</LinkTo>
<h2>Hello {{ this.sameName }}</h2>
<input type="text" value={{this.name}} {{on "input" this.updateName}}>
<BsButton @type="primary" {{on "click" this.hello}}>Click me</BsButton>

{{!-- Feel free to remove this! --}}


{{outlet}}